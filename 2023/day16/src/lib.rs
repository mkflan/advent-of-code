#![allow(unused)]

pub fn solve_part1(input: &str) -> usize {
    0
}

pub fn solve_part2(input: &str) -> usize {
    0
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_with_sample() {
        let sample_input = "";

        assert_eq!(0, solve_part1(sample_input));
        assert_eq!(0, solve_part2(sample_input));
    }
}
